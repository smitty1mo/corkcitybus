# NTA (National Transport Authority) GTFS-Realtime API key.
# Get one at https://developer.nationaltransport.ie/ by subscribing to the
# "GTFS-Realtime" product. Sent server-side only as the x-api-key header -
# never exposed to the browser.
NTA_API_KEY=
